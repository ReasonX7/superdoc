/**
 * These typedefs are provided for JS consumers and point at the canonical
 * TypeScript definitions in `core/types/ChainedCommands.ts`.
 */

/**
 * @typedef {import('../../types/ChainedCommands').CommandServiceOptions} CommandServiceOptions
 */

/**
 * @typedef {import('../../types/ChainedCommands').CoreCommands} CoreCommands
 */

/**
 * @typedef {import('../../types/ChainedCommands').ExtensionCommands} ExtensionCommands
 */

/**
 * @typedef {import('../../types/ChainedCommands').EditorCommands} EditorCommands
 */

/**
 * @typedef {import('../../types/ChainedCommands').ChainedCommand} ChainedCommand
 */

/**
 * @typedef {import('../../types/ChainedCommands').ChainableCommandObject} ChainableCommandObject
 */

/**
 * @typedef {import('../../types/ChainedCommands').CanCommand} CanCommand
 */

/**
 * @typedef {import('../../types/ChainedCommands').CanCommands} CanCommands
 */

/**
 * @typedef {import('../../types/ChainedCommands').CanObject} CanObject
 */

/**
 * @typedef {import('../../types/ChainedCommands').CommandProps} CommandProps
 */

/**
 * @typedef {import('../../types/ChainedCommands').Command} Command
 */

export {};
